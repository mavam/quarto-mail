pre-push:
  parallel: true
  commands:
    snippets:
      run: python3 -m json.tool _extensions/mail/_snippets.json >/dev/null
    changelog-validate:
      glob: "changelog/**"
      run: uvx tenzir-ship validate --lenient
